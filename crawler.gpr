with "ncursesada";

project Crawler is

   for Object_Dir use "obj";
   for Source_Dirs use ("src");
   for Exec_Dir use ".";
   for Main use ("crawler");

   package Compiler is
      for Default_Switches ("ada") use ("-gnat12");
   end Compiler;

end Crawler;

